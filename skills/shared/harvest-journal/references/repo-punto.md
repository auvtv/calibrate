# Mapa repo → punto de atención

Derivado de `calibrate/sistema-atencion/registro-atencion.yaml` (FUENTE de verdad).
Si editás el registro, actualizá esto. Si un repo no está acá, falta en el registro.

| repo (carpeta en /Users/andresusera/4consultoria) | punto |
|---|---|
| `genexa` | `genexa` |
| `garrapattack` | `garrapata` |
| `gale` | `prili-importaciones` (alias galenfix) |
| `prili` | `prili-planta` |
| `gestiongastos` | `lvtgrp` |
| `shadowbot` | `shadowbot` |
| `trini` | `trini` |
| `canasta` | `canasta` |
| `calibrate` | `calibrate` |
| `infra` | `infra` |
| `engram` | `engram` |

Notas:
- `prili` contiene planta + el scanning origen de `scanner-convexo`.
- `gale` = Galenfix, la importadora detrás de las ops de import de Prili.
- Puntos sin repo homónimo (no harvesteables desde un repo): observatorio, scanner-convexo,
  panel-instagram, kgis, convexo-maquina-agentica, plataforma-plugins, carape-*, convexo-colab,
  prili-agentico, prili-agente — su trabajo es de chat/diseño, no de un repo de código.
